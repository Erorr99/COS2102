        // double midX = (d1.x + d2.x) / 2.0;
        // double midY = (d1.y + d2.y) / 2.0;
        // string name = "midpoint(" + d1.name1 + "," + d2.name1 + ")";
        // Point midPoint(midX, midY, name);
        // return midPoint;